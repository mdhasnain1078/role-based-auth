<!DOCTYPE html>
<html>
<head><title>Dashboard</title></head>
<body>
  <h2>Welcome <%= user.role %></h2>
  <p>You are logged in with role: <%= user.role %></p>
  <a href="/logout">Logout</a><br>
  <% if (user.role === "admin") { %>
    <p>You are admin i.e this is visible to you</p>
  <% } %>
</body>
</html>
